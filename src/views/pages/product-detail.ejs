<section class="container my-4">
  <div class="row">

    <div class="col-md-6 text-center">
      <img src="<%= product.image %>" alt="<%= product.name %>"
           class="img-fluid"
           style="max-height: 400px; object-fit:cover;">
    </div>

    <div class="col-md-6">
      <h2><%= product.name %></h2>
      <p class="text-danger fw-bold fs-4">
        <%= product.price.toLocaleString() %> đ
      </p>

      <p><%= product.description %></p>

      <form action="/api/cart/add" method="POST" class="mt-3">
        <input type="hidden" name="productId" value="<%= product._id %>">

        <div class="d-flex align-items-center">
          <span class="me-2">Số lượng:</span>
          <input type="number" name="quantity" value="1" min="1"
                 class="form-control w-25 text-center">
        </div>

        <button type="submit" class="btn btn-danger mt-3 w-100">
          🛒 Thêm vào giỏ hàng
        </button>
      </form>

      <a href="javascript:history.back()" class="btn btn-outline-dark mt-2 w-100">
        ← Quay lại
      </a>
    </div>

  </div>
</section>
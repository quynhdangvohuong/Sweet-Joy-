<div class="container my-5">
  <h2 class="mb-4" style="font-family: 'Pacifico'">🛒 Giỏ hàng</h2>

  <% if (cart.length === 0) { %>
  <p class="text-muted">Giỏ hàng trống 😢</p>
  <a href="/" class="btn btn-danger">Tiếp tục mua sắm</a>
  <% } else { %>

  <table class="table">
    <thead>
      <tr>
        <th>Sản phẩm</th>
        <th>Giá</th>
        <th>Số lượng</th>
        <th>Hành động</th>
      </tr>
    </thead>
    <tbody>
      <% cart.forEach(function(item) { %>
      <tr>
        <td><%= item.product.name %></td>

        <td><%= item.product.price.toLocaleString() %> đ</td>

        <td><%= item.quantity %></td>

        <td>
          <!-- Tăng -->
          <form action="/api/cart/update" method="POST" style="display: inline">
            <input
              type="hidden"
              name="productId"
              value="<%= item.product._id %>"
            />
            <input type="hidden" name="action" value="increase" />
            <button class="btn btn-sm btn-success">+</button>
          </form>

          <!-- Giảm -->
          <form action="/api/cart/update" method="POST" style="display: inline">
            <input
              type="hidden"
              name="productId"
              value="<%= item.product._id %>"
            />
            <input type="hidden" name="action" value="decrease" />
            <button class="btn btn-sm btn-warning">−</button>
          </form>
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>

  <h4 class="text-end">Tổng tiền: <%= total.toLocaleString() %> đ</h4>

  <h4 class="text-end mt-3">
    Tổng tiền:
    <span class="text-danger fw-bold fs-4">
      <%= total.toLocaleString() %> đ
    </span>
  </h4>

  <div class="text-end mt-4">
    <a href="/" class="btn btn-outline-secondary me-2">← Mua thêm</a>
    <a href="/checkout" class="btn btn-danger">Thanh toán</a>
  </div>

  <% } %>
</div>
